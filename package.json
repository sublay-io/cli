{
  "name": "sublay-cli-workspace",
  "version": "7.0.0",
  "private": true,
  "description": "Sublay CLI and Component Registry",
  "scripts": {
    "build": "pnpm --filter @sublay/cli build",
    "dev": "pnpm --filter @sublay/cli dev",
    "publish-cli": "pnpm --filter @sublay/cli publish",
    "cli": "node packages/cli/dist/index.js"
  },
  "keywords": [
    "sublay",
    "cli",
    "components",
    "react",
    "react-native"
  ],
  "author": "Sublay",
  "license": "Apache-2.0",
  "devDependencies": {
    "nativewind": "^4.2.3",
    "typescript": "^6.0.3"
  },
  "pnpm": {
    "overrides": {
      "react": "19.2.5",
      "react-dom": "19.2.5"
    }
  }
}
